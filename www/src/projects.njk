---
layout: base.njk
title: Projects
description: Portfolio of software development projects and work
---

<section>
    <h1>Projects</h1>
    <p>Here are some of the projects I've worked on. Each project showcases different skills and technologies.</p>
</section>

<div class="projects-grid">
    {%- for project in collections.projects -%}
    <a href="{{ project.url }}" class="card project-card">
        <h3>{{ project.data.title }}</h3>
        <p class="blog-post-excerpt">{{ project.data.description }}</p>
        {% if project.data.tech %}
        <div class="project-tech">
            {% for tech in project.data.tech %}
            <span class="tech-tag">{{ tech }}</span>
            {% endfor %}
        </div>
        {% endif %}
    </a>
    {%- endfor -%}
</div>

{% if collections.projects.length == 0 %}
<p class="blog-post-excerpt">Projects will be added soon. Check back later!</p>
{% endif %}
