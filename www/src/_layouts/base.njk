<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ description or 'Software Developer Portfolio - Building digital products with modern web technologies' }}">
    <meta name="author" content="Leniel Luzardo">
    
    <!-- Open Graph / Social Media -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description or 'Software Developer Portfolio' }}">
    
    <link rel="stylesheet" href="/style.css">
    <title>{{ title }} | Leniel Luzardo</title>
</head>
<body>
    {% include "./header.njk" %}

    <main>
        {{ content | safe }}
    </main>

    {% include "./footer.njk" %}
</body>
</html>